import { betterAuthCapabilities } from "@/lib/capabilities";

export default function CapabilitiesPage() {
  return <><p className="text-[10px] font-medium uppercase tracking-[0.18em] text-emerald-700">Platform inventory</p><h1 className="mt-1.5 text-xl font-normal tracking-tight">Better Auth capabilities</h1><p className="mt-1.5 max-w-2xl text-sm text-slate-500">A visible inventory of what this identity deployment exposes, what is configurable now, and what still requires an explicit plugin rollout.</p><div className="mt-6 overflow-hidden rounded-xl border border-slate-200 bg-white">{betterAuthCapabilities.map(([name, description, status]) => <div className="flex flex-wrap items-center justify-between gap-4 border-b border-slate-100 px-4 py-3 last:border-0" key={name}><div><p className="text-sm font-medium text-slate-800">{name}</p><p className="mt-0.5 text-xs text-slate-500">{description}</p></div><span className={`rounded-full px-2.5 py-1 text-[10px] font-medium uppercase tracking-wide ${status === "enabled" ? "bg-emerald-50 text-emerald-700" : status === "available" ? "bg-slate-100 text-slate-600" : "bg-amber-50 text-amber-700"}`}>{status}</span></div>)}</div></>;
}
