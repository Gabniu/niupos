name: nova_mobile
description: NOVA POS Flutter-ready offline synchronization core.
publish_to: none
version: 0.1.0+1

environment:
  sdk: ">=3.6.0 <4.0.0"

dev_dependencies:
  lints: ^5.1.1
  test: ^1.25.15
